# Commons

De manière générale, il faut _éviter_ de créer des classes utilitaires et préférer
l'implémentation de méthodes utilitaires directement dans les objets impactés par
ces méthodes. Le module `commons` n'est pas suffisamment spécifique et ne contient
de manière générale que très peu de choses.